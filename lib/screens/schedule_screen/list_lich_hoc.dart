import 'package:flutter/material.dart';

class ListLichHoc extends StatelessWidget {
  const ListLichHoc({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
